Primary lookup before run_command. Search cached UIDs, names, addresses, notes, module names, and complete function signatures using a device or capability phrase. Results include device identity and relevant modules with full signatures and available reference docs. Fuzziness is 0 for literal matching, 1 for conservative typo tolerance, or 2 for broader recovery. The tool reads only cache data unless `status` or `includeStatus` requests live reachability checks. If expected data is missing, refresh it with `discover_devices` or `discover_commands` and search again.
